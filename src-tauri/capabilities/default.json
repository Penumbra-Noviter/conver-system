{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：核心 API 默认权限；放行本地后端远程 URL http://127.0.0.1:*（自定义命令对应用窗口默认可用，见 spec R3）",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  },
  "permissions": ["core:default"]
}
