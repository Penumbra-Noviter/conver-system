# Conver System 移动端 — 安全 (SECURITY)

> 移动端安全模型与边界声明（2026-08-28 建库，源自设计文档 §4.5 决策与 grilling Q5 拍板）。总体原则：**比桌面更紧**。

## 威胁模型

单用户自用应用。可信面：用户本人持有的设备；设备上运行的游戏 HTML 视为**不可信代码**（第三方单文件 HTML，含导入与 AI 生成），主应用数据与其物理隔离。

## 边界契约

1. **API Key 存储**：只存系统安全存储（iOS Keychain / Android Keystore，经 `flutter_secure_storage`），不落 SQLite、不落日志。
2. **Key 注入**：仅当游戏运行且须访问 LLM 时经 WebView `runJavaScript` 白名单三元组注入（endpoint/model/Key）；**claude key 恒不进游戏**（与桌面既有规定一致）。
3. **游戏与主应用隔离**：
   - 游戏在独立 origin（本地 HTTP 服务器 `127.0.0.1:<固定端口>`，明文流量限制在回环）运行，摸不到主应用数据目录、数据库、SecureStorage。
   - 存档 localStorage 单 origin + **前缀隔离**：键复用 saveKeys 白名单语义，注入幂等守卫防越界。
4. **导入安全**：文件名净化 / SHA-256 去重 / cfg 探测 / 恶意粗筛（全量移植桌面导入校验链）；命中恶意模式 → **拒绝 + 显示命中关键词清单 + 强制二次确认**（知情才放行）。
5. **明文流量**：本地 HTTP 服务器仅监听 127.0.0.1 固定端口；公网 LLM 调用一律 HTTPS。不出设备的明文仅在回环。
6. **数据主权**：全部数据本地存储，无云依赖；导出（角色卡/对话/存档）格式对齐桌面，可双端互迁，用户自行掌控。
7. **凭证不进版本库**：Key/密钥不写代码，不入 git（`.gitignore` 已排除 `.env*`、签名文件）。

## 报告渠道

发现安全问题（自用项目亦不例外）：记录到 `desktop/TECH_DEBT.md`（若涉及两端）或本库 `TECH_DEBT.md`（仅移动端），附复现路径与影响评估。