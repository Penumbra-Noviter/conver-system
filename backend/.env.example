# LLM API Keys（至少配置一个）
CLAUDE_API_KEY=
OPENAI_API_KEY=

# OpenAI 兼容 API 的自定义地址（可选，留空则使用默认 https://api.openai.com/v1）
OPENAI_BASE_URL=

# 默认模型设置
DEFAULT_PROVIDER=claude
DEFAULT_MODEL=claude-sonnet-5

# 数据库路径（默认项目根目录）
DATABASE_URL=sqlite+aiosqlite:///./conver_system.db
